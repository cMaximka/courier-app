<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CourierApp">
        <activity
            android:name=".presentation.ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.OrderDetailActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.OrderAdapter"
            android:exported="false" />
        <activity
            android:name=".presentation.CreateOrderActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.CourierActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ClientActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.CourierApp" />
        <activity
            android:name=".presentation.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>